DROP TABLE IF EXISTS wp_tsa_skater_shootout;

CREATE TABLE wp_tsa_skater_shootout (
    gameDate DATE,
    gameId BIGINT,
    skaterFullName VARCHAR(150),
    playerId BIGINT,
    homeRoad VARCHAR(10),
    teamAbbrev VARCHAR(10),
    opponentTeamAbbrev VARCHAR(10),
    shootsCatches VARCHAR(10),
    positionCode VARCHAR(10),

    shootoutGamesPlayed INT,
    shootoutGoals INT,
    shootoutShots INT,
    shootoutShootingPct DECIMAL(10,4),
    shootoutGameDecidingGoals INT,

    careerShootoutGamesPlayed INT,
    careerShootoutGoals INT,
    careerShootoutShots INT,
    careerShootoutShootingPct DECIMAL(10,4),
    careerShootoutGameDecidingGoals INT,

    lastName VARCHAR(100),

    INDEX idx_gameDate (gameDate),
    INDEX idx_gameId (gameId),
    INDEX idx_playerId (playerId),
    INDEX idx_teamAbbrev (teamAbbrev),
    INDEX idx_opponentTeamAbbrev (opponentTeamAbbrev),
    INDEX idx_positionCode (positionCode),
    INDEX idx_homeRoad (homeRoad),
    INDEX idx_skaterFullName (skaterFullName)
);

DELETE FROM wp_tsa_skater_shootout
WHERE skaterFullName = 'skaterFullName';