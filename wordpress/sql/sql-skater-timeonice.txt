DROP TABLE IF EXISTS wp_tsa_skater_timeonice;

CREATE TABLE wp_tsa_skater_timeonice (
    gameDate DATE,
    gameId BIGINT,
    skaterFullName VARCHAR(150),
    playerId BIGINT,
    homeRoad VARCHAR(10),
    teamAbbrev VARCHAR(10),
    opponentTeamAbbrev VARCHAR(10),
    shootsCatches VARCHAR(10),
    positionCode VARCHAR(10),
    gamesPlayed INT,

    timeOnIce INT,
    evTimeOnIce INT,
    ppTimeOnIce INT,
    shTimeOnIce INT,

    timeOnIcePerGame DECIMAL(10,4),
    evTimeOnIcePerGame DECIMAL(10,4),
    ppTimeOnIcePerGame DECIMAL(10,4),
    shTimeOnIcePerGame DECIMAL(10,4),

    otTimeOnIce INT,
    otTimeOnIcePerOtGame DECIMAL(10,4),

    shifts INT,
    timeOnIcePerShift DECIMAL(10,4),
    shiftsPerGame DECIMAL(10,4),

    lastName VARCHAR(100),

    INDEX idx_gameDate (gameDate),
    INDEX idx_gameId (gameId),
    INDEX idx_playerId (playerId),
    INDEX idx_teamAbbrev (teamAbbrev),
    INDEX idx_opponentTeamAbbrev (opponentTeamAbbrev),
    INDEX idx_positionCode (positionCode),
    INDEX idx_homeRoad (homeRoad),
    INDEX idx_skaterFullName (skaterFullName)
);

DELETE FROM wp_tsa_skater_timeonice
WHERE skaterFullName = 'skaterFullName';